/** Persisted as PostgreSQL enum `conversation_follow_up_status_enum`. */
export enum ConversationFollowUpStatus {
  pending = "PENDING",
  sent = "SENT",
  cancelled = "CANCELLED",
  failed = "FAILED",
}
