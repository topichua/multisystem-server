/** Persisted as PostgreSQL enum `orders_delivery_status_enum`. */
export enum OrderDeliveryStatus {
  pending = "pending",
  shipped = "shipped",
  delivered = "delivered",
  returned = "returned",
}
