/** Persisted as PostgreSQL enum `automation_scheduled_job_status_enum`. */
export enum AutomationScheduledJobStatus {
  pending = "PENDING",
  sent = "SENT",
  cancelled = "CANCELLED",
  failed = "FAILED",
}
