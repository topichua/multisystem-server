/** Persisted as PostgreSQL enum `order_delivery_provider_enum`. */
export enum OrderDeliveryProvider {
  nova_poshta = "nova_poshta",
  ukrposhta = "ukrposhta",
  manual = "manual",
  other = "other",
}
